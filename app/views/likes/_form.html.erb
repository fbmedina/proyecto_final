<%= form_for [@store, likeable, Like.new] do |f| %>
    <%= f.hidden_field :user_id %>
    <% if likeable.liked_by? current_user %>
    	<%= f.submit "", class: "star selected" %>
    <% else %>
    	<%= f.submit "", class: "star unselected" %>
    <% end %>
<% end %>